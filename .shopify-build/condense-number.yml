containers:
  default:
    docker: circleci/openjdk:10-node-browsers

steps:
- label: 'Tests 👓'
  cache:
    - build/cache/jest
  dependencies:
  - yarn
  run:
    - yarn jest
  timeout: 5m

- label: 'Lint 💅'
  dependencies:
  - yarn
  run:
    - yarn eslint . --ext .ts
  timeout: 5m
